// ////////////////////////////////////////////////////////////////////////////

// example 4: default exports:

// default exports for feed config
const feedConfig = {
  maxPosts: 10,
  theme: 'light',
  cacheTimeout: 300000, // 5 min (60 x 5 x 1000 = 300,000)
};

export default feedConfig;
